require 'spec_helper'

describe GoogleAppsManager do
  it 'has a version number' do
    expect(GoogleAppsManager::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
